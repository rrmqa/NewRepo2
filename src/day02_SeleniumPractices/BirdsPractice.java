package day02_SeleniumPractices;

public class BirdsPractice {
    /*
    WebDriverManager.chromedriver().setup();
        WebDriver driver = new ChromeDriver();

        String[] data = { "red birds", "yellow birds", "colorful butterflies"}; // SINGING BIRDS BEAUTIFULLY
        String[] title = new String[3];
        ArrayList<String> URL = new ArrayList<String>();


        int i=0;
        for (String each : data) {
            driver.get("https://google.com");
            driver.findElement(By.name("q")).sendKeys(each + Keys.ENTER);
            driver.findElement(By.partialLinkText(each)).click();
            //Thread.sleep(1000);
            title[i] = driver.getTitle();
            URL.add(driver.getCurrentUrl());

            i++;
        }
        System.out.println(Arrays.toString(title));
        System.out.println(URL);


        driver.close();
     */
}
